[project]
name = "clinical-trial-pipeline"
version = "0.1.0"
description = "ELT data pipeline for clinical trial data from ClinicalTrials.gov"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
